package com.macieklato.ragetracks.test;

import com.macieklato.ragetracks.controller.SongController;
import com.macieklato.ragetracks.model.Song;
import com.macieklato.ragetracks.util.Network;

import junit.framework.TestCase;

public class SongControllerTest extends TestCase {

	/*
	 * SongController sc; Song song; Song song2; Song song3;
	 * 
	 * private void clean() { sc = SongController.getInstance(); sc.reset();
	 * sc.reset(); song = new Song( 31498,
	 * "http://RageTracks.com/revolvr-genesis-unstoppable-ft-splitbreed/",
	 * "Revolvr & Genesis", "Unstoppable ft. Splitbreed",
	 * String.format("http://api.soundcloud.com/tracks/%d/stream?client_id=%s",
	 * 146449258, Network.CLIENT_ID),
	 * "http://RageTracks.com/wp-content/uploads/2014/04/artworks-000077573308-cq6dct-t500x500-300x300.jpg"
	 * , 1, 1, "146449258"); song2 = new Song( 31495,
	 * "http://RageTracks.com/tristam-braken-frame-of-mind/",
	 * "Tristam & Braken", "Frame of Mind",
	 * String.format("http://api.soundcloud.com/tracks/%d/stream?client_id=%s",
	 * 146330214, Network.CLIENT_ID),
	 * "http://RageTracks.com/wp-content/uploads/2014/04/artworks-000077502172-m6qec5-t500x500-300x300.jpg"
	 * , 1, 2, "146330214"); song3 = new Song(); }
	 * 
	 * public void testUninitializedState() { clean();
	 * assertEquals(SongController.UNINITIALIZED, sc.getState()); }
	 * 
	 * public void testMustLoad() { clean();
	 * assertEquals(SongController.UNINITIALIZED, sc.getState()); sc.toggle();
	 * assertEquals(SongController.UNINITIALIZED, sc.getState()); }
	 * 
	 * public void testLoad() { clean();
	 * assertEquals(SongController.UNINITIALIZED, sc.getState());
	 * sc.toggle(song); assertEquals(SongController.LOADING, sc.getState()); }
	 * 
	 * public void testSong() { clean(); sc.toggle(song);
	 * assertTrue(song.equals(sc.getSong()));
	 * assertFalse(song2.equals(sc.getSong()));
	 * assertEquals(SongController.LOADING, sc.getState()); sc.toggle(song2);
	 * assertTrue(song.equals(sc.getSong()));
	 * assertFalse(song2.equals(sc.getSong())); }
	 */
}
